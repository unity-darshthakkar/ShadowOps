# Project Documentation Rules (Non-Obvious Only)

- `SHADOWOPS_BRIEF.md` is the canonical spec — treat it as the source of truth for feature scope.
- "AI Tax" is a project-specific term defined in the brief, not a standard industry term — explain it when surfacing it in documentation.
- The 4 workflow types (official, actual, AI-automated, hybrid) are the core conceptual model; all features should map back to one or more of these.
- No source code exists yet — answers about implementation must be framed as "planned" unless code has since been added.
